# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 |  | architecture | ClawDeck bridge lifecycle management | Session Deck manages bridge start/stop via SSH to Windows | Session Deck already deploys configs to Windows via SSH/SCP. Managing the bridge process is a natural extension, avoids requiring manual bridge restart after config changes. | Yes | human |
| D002 |  | architecture | Project repository structure | Separate repo from ClawDeck | Session Deck is a companion configuration tool, not part of the runtime controller. Separate lifecycle, deployment, and concerns. Decision carried forward from ClawDeck project. | No | human |
| D003 |  | architecture | wezterm.lua modification strategy | Marked section approach: `-- SESSION-DECK-START` / `-- SESSION-DECK-END` | Preserves all user customizations (R019). Only the workspace section between markers is replaced. Easy to detect conflicts (R021) and back up (R022). | Yes | agent |
| D004 |  | library | Frontend framework | Svelte for the web frontend | User chose Svelte for its excellent DX, small bundle output, and strong reactivity model. Good fit for the interactive drag-drop grid editor and real-time status updates. Requires build step (Vite) but produces optimal output. | No | human |
| D005 |  | architecture | Product direction — config tool vs web terminal | Web-native terminal workspace manager with xterm.js. Drop WezTerm config generation, drop ClawDeck integration for now. | Jesse only used WezTerm as a means to manage tmux sessions. The real goal is tmux session management and viewing. No existing tool provides a multi-pane web dashboard for tmux. Building with xterm.js eliminates the config generation pipeline entirely. ClawDeck can be refactored later to send HTTP requests to switch workspaces. | No | human |
| D006 |  | architecture | Terminal connection lifecycle strategy | Lazy connect — only active workspace has live WebSocket/terminal connections | With 3-4 workspaces × 2-10 panes, keeping all connections live (up to 40) wastes resources. Since tmux is persistent, reconnecting on workspace switch is instant. Only 2-10 connections live at a time. | Yes | collaborative |
| D007 |  | architecture | Input and navigation model | Keyboard-first navigation with mouse support. ClawDeck refactor deferred to future milestone. | User prioritizes keyboard shortcuts for workspace/pane switching over Stream Deck integration. ClawDeck can be refactored later to send HTTP to Session Deck. | Yes | human |
| D008 |  | architecture | Copy/paste behavior in terminal panes | Clean copy/paste is a first-class requirement. xterm.js selection must produce clean text without line-break artifacts, decoration, or padding. Will need custom selection handling and clipboard API integration. | WezTerm copy/paste produces messy output with extra line breaks and indentation from visual borders. This is a daily pain point. The web terminal must do better — clipboard content should match raw terminal output exactly. | No | human |
| D009 |  | architecture | Workspace layout data model | Binary split tree model — recursive horizontal/vertical splits with flex sizing, not fixed grids | User needs arbitrary layouts from 2x1 up to 4x3 with mixed combinations. Fixed grid is too rigid. Split tree mirrors tmux's own model, supports any combination of nested horizontal/vertical splits with proportional sizing, and enables drag-to-resize borders. | No | collaborative |
